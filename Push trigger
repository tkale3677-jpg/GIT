name: Push Trigger Workflow
on:
  push:
   branches:
      -main
jobs:
 push-job:
   runs-on: ubuntu-latest
   steps:
     -name: Print Success Message
      run: echo "Push event triggered successfully!"
     -name: Display date and time
      run: date
     -name:Display current working directory
      run: pwd
